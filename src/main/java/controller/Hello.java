package controller;import javax.servlet.ServletException;import javax.servlet.annotation.WebServlet;import javax.servlet.http.HttpServlet;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import java.io.IOException;import java.io.PrintWriter;@WebServlet (  name = "Hello",  urlPatterns = {"/greeting", "/Hello"},  loadOnStartup = 1)public class Hello extends HttpServlet {  private final String DEFAULT_USER = "dear";  @Override  public void init() throws ServletException {  }  @Override  protected void doGet(HttpServletRequest req, HttpServletResponse res) throws ServletException, IOException {    doPost(req, res);  }  @Override  protected void doPost(HttpServletRequest req, HttpServletResponse res) throws ServletException, IOException {    req.setCharacterEncoding("UTF-8");    String name = req.getParameter("name");    if(name == null || name == "") name = DEFAULT_USER;//    System.out.println(name);    res.setContentType("text/html;charset=UTF-8");    res.setCharacterEncoding("UTF-8");//    res.getWriter().println(this.getServletName() + " say: " + "Hello," + name + "!");    PrintWriter writer = res.getWriter();    writer.append("<!DOCTYPE html><html><body>")            .append("Hello, ").append(name).append("!\r\n")            .append("<form action=\"greeting\" method=\"post\">")            .append("Enter your name:<br>")            .append("<input name=\"name\"><br/>")            .append("<input type=\"submit\" value=\"Submit\">")            .append("</form>")            .append("</body></html>");  }  @Override  public void destroy() {  }}